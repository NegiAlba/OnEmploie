{% extends "base.html.twig" %}

{% block title %}Create a Job offer !
{% endblock %}

{% block body %}
	<h1>Create a Job Offer</h1>

	<div class="container">
		{{form_start(formulaire)}}
		{{form_row(formulaire, {'attr': {'class': 'border-4'}})}}
		<button type="submit" class="btn btn-secondary">Create</button>
		{{form_end(formulaire)}}
	</div>
{% endblock %}
